# KanbanBoard

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-25 16:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a full-stack project management application organized as a multi-project .NET solution. The core backend is KanbanBoard.Api, a REST API that manages kanban board entities (projects, epics, work items, comments, and epic documents) using SQLite as its data store via Entity Framework Core. KanbanBoard.Shared provides common types, models, and data transfer objects shared across the solution's projects. KanbanBoard.Mcp is a Model Context Protocol (MCP) server that wraps the API's functionality and exposes it as callable tools for LLM-based AI assistants, enabling AI agents to programmatically query and manipulate the kanban board. The three projects work together in a layered fashion: Shared defines the common contracts, Api implements the HTTP endpoints and data persistence, and Mcp bridges AI tooling to the Api. There are no identified external repositories that depend on this one.

## Projects

### KanbanBoard.Api

KanbanBoard.Api appears to be the web API layer of a Kanban board application, responsible for exposing HTTP endpoints and handling client requests. It likely serves as the entry point for the application, coordinating between the presentation layer and underlying business logic or data access layers in the repository.

### KanbanBoard.Mcp

KanbanBoard.Mcp appears to be a Model Context Protocol (MCP) integration project for the KanbanBoard repository. Based on the project name, it likely exposes KanbanBoard functionality as MCP tools or resources, enabling AI assistants to interact with the kanban board system. The graph contains no class/interface nodes to analyze, suggesting the project may be minimal, auto-generated, or primarily configuration-based.

### KanbanBoard.Shared

KanbanBoard.Shared is a shared library project within the KanbanBoard repository that contains common types, models, and utilities used across other projects in the solution. Based on its name and role, it likely defines data transfer objects, domain models, or shared interfaces that are referenced by both client and server projects in a Kanban board application.

### KanbanBoard

KanbanBoard is a full-stack project management application consisting of a REST API backend (KanbanBoard.Api) backed by a SQLite database via Entity Framework Core, and an MCP (Model Context Protocol) server (KanbanBoard.Mcp) that exposes the API's capabilities as tools callable by LLM assistants. The API manages projects, epics, work items, comments, and epic documents, while the MCP layer wraps these operations so AI agents can programmatically query and manipulate the kanban board.

