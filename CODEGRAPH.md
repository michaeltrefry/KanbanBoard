# KanbanBoard

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-21 10:02 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a full-stack kanban board application composed of two main projects. KanbanBoard.Api is an ASP.NET Core REST API that manages a hierarchical data model of projects, epics, work items, epic documents, and comments, persisted in SQLite via Entity Framework Core with cascading delete relationships. It also exposes a real-time change notification mechanism using in-process channels, allowing subscribers to receive board change events. KanbanBoard.Mcp is a Model Context Protocol (MCP) tool layer that wraps the API's HTTP endpoints as structured, LLM-callable tools, enabling AI assistants (such as those using the MCP standard) to programmatically perform CRUD operations on all kanban entities. The two projects work together such that the MCP layer acts as an AI-oriented facade over the REST API: the API handles data persistence and business logic while the MCP layer translates AI assistant requests into API calls. The repository has no detected cross-repository dependencies; it relies on ASP.NET Core, Entity Framework Core (SQLite provider), and an MCP SDK for its core functionality. Nothing external is known to depend on this repository.

## Projects

### KanbanBoard

KanbanBoard is a full-stack kanban board application consisting of a backend REST API (KanbanBoard.Api) backed by SQLite via Entity Framework Core, and an MCP (Model Context Protocol) tool layer (KanbanBoard.Mcp) that exposes the API's capabilities as LLM-callable tools. The API manages projects, epics, work items, epic documents, and comments with cascading delete relationships; the MCP layer wraps these endpoints so AI assistants can programmatically create, read, update, and delete kanban entities. A real-time change notification mechanism via in-process channels allows subscribers to receive board change events.

