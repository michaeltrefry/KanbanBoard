# KanbanBoard

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-19 12:07 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a self-contained task and project management system implemented in .NET. It is structured around three layers: (1) a core domain/data layer that models work as Projects, Epics, EpicDocuments, and WorkItems stored in a SQLite database, with attributes such as status, priority, type, ordering, and labels; (2) KanbanBoard.Api, which exposes this domain via an HTTP REST API; and (3) KanbanBoard.Mcp, which implements a Model Context Protocol (MCP) tool layer enabling AI assistants to interact with the board programmatically. A real-time change notification mechanism built on System.Threading.Channels propagates board updates internally. A static initializer manages SQLite schema migrations and seed data. The three projects are tightly coupled: the MCP layer and the API layer both depend on the shared domain/data layer, while the MCP layer provides an alternative, AI-oriented interface to the same underlying data. There are no observed cross-repository dependencies; this repository is self-sufficient. External runtime dependencies include SQLite (for persistence) and the .NET MCP SDK (for AI tool integration).

## Projects

### KanbanBoard

KanbanBoard is a task and project management application exposing an HTTP REST API (KanbanBoard.Api) backed by a SQLite database, plus an MCP (Model Context Protocol) tool layer (KanbanBoard.Mcp) that allows AI assistants to interact with the board programmatically. The core data model organizes work into Projects, Epics, EpicDocuments, and WorkItems with statuses, priorities, types, ordering, and labels. A real-time change notification mechanism using System.Threading.Channels supports live board updates, and a static initializer handles schema migrations and seed data.

