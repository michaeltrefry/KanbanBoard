# KanbanBoard

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-25 12:39 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a full-stack project management application organized as a multi-project .NET solution. The core system consists of three projects that collaborate to provide both human-facing and AI-agent-facing interfaces to kanban-style workflow data.

KanbanBoard.Api is the REST backend, built with ASP.NET Core and Entity Framework Core using a SQLite database. It exposes HTTP endpoints for managing projects, epics, work items (with statuses, priorities, ordering, and labels), and comments. It also supports server-sent events for real-time change notifications.

KanbanBoard.Mcp is a Model Context Protocol (MCP) server that wraps the same domain logic and exposes all CRUD and query operations as tool-callable endpoints, enabling LLM agents (such as those built with Claude or similar AI assistants) to interact programmatically with the kanban board system.

KanbanBoard.Shared is a shared library that provides common types, models, interfaces, or contracts referenced by both the API and MCP projects, acting as the foundational data contract layer across the solution.

The three projects together implement a dual-interface architecture: one surface for conventional HTTP clients and one for AI tooling. The system has no observed cross-repository dependencies and appears to be self-contained. External dependencies include Entity Framework Core (SQLite provider), ASP.NET Core, and the MCP SDK for .NET.

## Projects

### KanbanBoard.Api

KanbanBoard.Api appears to be the web API layer of a Kanban board application, responsible for exposing HTTP endpoints that allow clients to interact with kanban board data. Based on the project name and repository context, it likely serves as the backend service handling requests related to boards, columns, and cards/tasks typical of a Kanban-style project management tool.

### KanbanBoard.Mcp

KanbanBoard.Mcp appears to be a Model Context Protocol (MCP) integration project for a Kanban board application. Based on the project name, it likely exposes Kanban board functionality as MCP tools/resources, enabling AI assistants to interact with the board system. The project serves as a bridge layer connecting the core Kanban board domain logic with MCP-compatible AI tooling interfaces.

### KanbanBoard.Shared

KanbanBoard.Shared is a shared library project within the KanbanBoard repository, intended to contain common types, models, interfaces, or utilities that are referenced by other projects (such as client and server projects) within the solution. Based on the graph provided, no specific class or interface nodes were listed, suggesting this project may serve as a foundational shared contract layer with minimal or no currently defined types exposed in the graph.

### KanbanBoard

KanbanBoard is a full-stack project management application consisting of a REST API backend (KanbanBoard.Api) and an MCP (Model Context Protocol) tool server (KanbanBoard.Mcp). The API manages projects, epics, work items, and comments using Entity Framework Core with a SQLite database, while the MCP layer exposes all CRUD and query operations as tool-callable endpoints for LLM agents. The system supports a kanban-style workflow with statuses, priorities, ordering, labels, and real-time change notifications via server-sent channels.

