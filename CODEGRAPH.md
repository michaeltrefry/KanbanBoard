# KanbanBoard

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-18 15:55 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a self-contained project management system implementing a kanban workflow model. It consists of two integrated components: (1) an ASP.NET Core REST API backend that uses Entity Framework Core with SQLite for persistence, exposing CRUD operations over a hierarchical data model (Projects → Epics → WorkItems, with EpicDocuments attached to Epics); and (2) an MCP (Model Context Protocol) server that wraps the API's full CRUD surface as tool-callable endpoints, enabling AI assistants to interact with the kanban board programmatically. Work items are typed (Story/Issue/Task), carry status values (Backlog/Ready/InProgress/etc.), priority levels, ordering indices, and labels. The two components work together such that the MCP server acts as a structured bridge between AI tooling and the underlying REST API, allowing agents to create, read, update, and delete projects, epics, documents, and work items through standardized tool calls. The repository has no detected external cross-repository dependencies; it depends on standard .NET ecosystem packages (ASP.NET Core, EF Core, SQLite) and the MCP SDK for the protocol server layer.

## Projects

### KanbanBoard

KanbanBoard is a project management application built around a kanban workflow model. It consists of an ASP.NET Core API backend (using Entity Framework Core with SQLite) that manages Projects, Epics, EpicDocuments, and WorkItems, and an MCP (Model Context Protocol) server component that exposes the API's full CRUD surface as tool-callable endpoints for AI assistants. The data model supports hierarchical organization: Projects contain Epics and WorkItems, Epics contain WorkItems and Documents, with work items tracked by type (Story/Issue/Task), status (Backlog/Ready/InProgress/etc.), priority, ordering, and labels.

