# KanbanBoard

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-28 13:29 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a multi-project .NET solution implementing a Kanban-style task/work-item management system. The solution is composed of several cooperating projects: (1) KanbanBoard.Api is the core backend, providing HTTP endpoints for work item management, projects, epics, and documents, with Entity Framework Core and MariaDB for persistence, and supporting both OAuth/OIDC external identity providers and personal access tokens (PATs) for authentication. (2) KanbanBoard.DataMigrator handles database schema and data migrations, including SSH tunnel support for remote database access. (3) KanbanBoard.Mcp is a Model Context Protocol server that exposes KanbanBoard functionality as AI-accessible tools, allowing AI agents to perform CRUD operations on the system's entities via authenticated HTTP calls using PATs and an internal shared-secret mechanism. (4) KanbanBoard.Shared provides common contracts, models, and utilities shared across the solution to ensure consistency. (5) KanbanBoard.Tests covers unit and integration testing for security-critical components including authentication middleware, PAT encryption/validation/revocation, and MCP token authentication. The projects work together as a layered system: the API serves as the server-side foundation, the migrator manages its database schema, the shared library provides cross-cutting types, the MCP server wraps the API for AI agent consumption, and the test project validates correctness across layers. Externally, the KanbanBoard frontend or API makes HTTP calls to at least two other repositories: TC.CodeGraphApi (called multiple times, suggesting integration with a code graph or analysis service) and CodeGraph (also called multiple times), as well as DateAndRate. No other repositories are observed to depend on KanbanBoard based on the provided data.

## Dependencies (this service calls)

- **TC.CodeGraphApi**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=DELETE, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
- **DateAndRate**
  - HTTP_CALLS: http_method=GET, url_pattern=/, route_template=, confidence_band=medium
- **CodeGraph**
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=DELETE, url_pattern=/api/projects/{param}, route_template=api/projects/{name}, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium
  - HTTP_CALLS: http_method=GET, url_pattern=/api/projects, route_template=api/projects, confidence_band=medium

## Projects

### KanbanBoard

KanbanBoard is a project that appears to implement a Kanban board application, likely for task management with columns (e.g., To Do, In Progress, Done) and cards. Based on the repository name alone and the absence of provided graph nodes and source snippets, the technical responsibilities cannot be described in detail beyond what the name implies.

### KanbanBoard.Api

KanbanBoard.Api is the backend API layer of a Kanban board application. It provides data persistence via Entity Framework Core (with MariaDB support), authentication/authorization including OAuth/OIDC external identity integration and personal access tokens (PATs), and HTTP endpoints for user settings, work item management, and internal service communication. It serves as the server-side foundation consumed by a frontend or shared contracts layer.

### KanbanBoard.DataMigrator

KanbanBoard.DataMigrator is a database migration utility project responsible for managing schema and data migrations for a Kanban board application. It handles database connectivity including SSH tunnel support for secure remote database access, and provides schema definition helpers for Kanban-related tables.

### KanbanBoard.Mcp

KanbanBoard.Mcp is a Model Context Protocol (MCP) server project that exposes KanbanBoard functionality as AI-accessible tools. It provides an authenticated HTTP interface allowing AI agents to perform CRUD operations on projects, epics, work items, and documents within the KanbanBoard system, secured via personal access tokens and an internal API shared-secret mechanism.

### KanbanBoard.Shared

KanbanBoard.Shared is a shared library project within the KanbanBoard repository, likely containing common contracts, models, and utilities used across multiple projects (e.g., client and server). Its role is to provide shared types and definitions to avoid duplication and ensure consistency across the solution.

### KanbanBoard.Tests

KanbanBoard.Tests is the automated test project for the KanbanBoard application, containing unit and integration tests for authentication middleware, user identity management, personal access token (PAT) services, and MCP (Model Context Protocol) token authentication. It verifies security-critical behaviors such as antiforgery protection, authentication gating, PAT encryption/validation/revocation, and HTTP handler integration for internal API communication.

