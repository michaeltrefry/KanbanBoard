# KanbanBoard

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-25 22:04 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is an ASP.NET Core repository implementing a kanban-style project management board. The repository is organized into multiple projects: KanbanBoard (or KanbanBoard.Api) is the core web API layer backed by SQLite via Entity Framework Core, exposing HTTP endpoints to manage projects, epics, work items, and comments. It includes OpenID Connect authentication with cookie sessions, personal access tokens (PAT) secured with AES-GCM encryption, CSRF protection, and real-time board notifications via server-sent events. KanbanBoard.Mcp is an MCP (Model Context Protocol) server layer that wraps the API surface and exposes it to AI assistant clients such as Claude or OpenAI Codex, enabling programmatic interaction with the board through the MCP standard. KanbanBoard.Shared is a class library containing shared contracts, DTOs, or model types consumed by both the API and MCP projects to avoid duplication. Together, the projects form a self-contained kanban board system with a REST API as the core, an AI-assistant integration layer on top, and a shared contract library tying them together. No external downstream dependents were identified; the system depends on ASP.NET Core, Entity Framework Core (SQLite), and MCP tooling libraries.

## Projects

### KanbanBoard.Api

KanbanBoard.Api appears to be the web API layer of a Kanban board application, responsible for exposing HTTP endpoints that front-end clients or other consumers can call to manage Kanban board data. It likely sits on top of a domain/application layer and handles request routing, authentication, and serialization concerns.

### KanbanBoard.Mcp

KanbanBoard.Mcp appears to be a Model Context Protocol (MCP) integration project within a Kanban board application repository. Its role is likely to expose Kanban board functionality through the MCP standard, enabling AI assistants or other MCP-compatible clients to interact with the board's data and operations. Based on the project name and repository context, it serves as an MCP server layer for the KanbanBoard system.

### KanbanBoard.Shared

KanbanBoard.Shared is a shared library project within the KanbanBoard repository that likely contains common models, DTOs, interfaces, or utility types used across multiple projects (such as client and server). Its role is to provide a single source of truth for shared contracts and data structures to avoid duplication between projects.

### KanbanBoard

KanbanBoard is an ASP.NET Core web API that manages a kanban-style project board with projects, epics, work items, and comments, backed by a SQLite database via Entity Framework Core. It supports optional OpenID Connect authentication with cookie sessions, personal access tokens (PAT) with AES-GCM encryption for API access, CSRF protection, and real-time board change notifications via server-sent channels. An accompanying MCP (Model Context Protocol) tool layer exposes the full API surface to AI assistant clients such as Codex or Claude.

