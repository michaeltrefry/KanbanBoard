# KanbanBoard

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-26 04:03 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is an ASP.NET Core web API application backed by SQLite via Entity Framework Core that implements a kanban-style project management system. The core domain models include projects, epics, work items, comments, and epic documents. The solution is composed of multiple projects that work together: KanbanBoard (or KanbanBoard.Api) provides the HTTP API layer with endpoints for managing all kanban entities, supporting OpenID Connect authentication with cookie sessions, CSRF protection, and personal access tokens (PAT) for API-key-style access; KanbanBoard.Shared contains common data transfer objects, models, or interfaces shared across projects; and KanbanBoard.Mcp is a Model Context Protocol integration layer that exposes the full API surface as typed MCP server tools, enabling LLM agents and AI assistants to interact with the kanban board system programmatically. The repository has no identified cross-repository dependencies. Externally it depends on ASP.NET Core, Entity Framework Core (SQLite provider), and MCP tooling libraries. Nothing external is known to depend on this repository.

## Projects

### KanbanBoard.Api

KanbanBoard.Api appears to be a web API project serving as the backend for a Kanban board application. It likely exposes HTTP endpoints for managing kanban board entities (such as boards, columns, and cards) and acts as the primary interface layer between clients and the application's business logic.

### KanbanBoard.Mcp

KanbanBoard.Mcp appears to be a Model Context Protocol (MCP) integration project for the KanbanBoard repository. Based on the naming convention, it likely exposes KanbanBoard functionality as MCP tools or resources, enabling AI assistants to interact with the Kanban board system. The project serves as a bridge between the core KanbanBoard domain logic and MCP-compatible AI tooling infrastructure.

### KanbanBoard.Shared

KanbanBoard.Shared is a shared library project within the KanbanBoard repository. Based on its name and role in the solution structure, it likely contains common data transfer objects, models, enumerations, interfaces, or utility types that are shared between other projects (such as client and server) in the KanbanBoard application. No graph nodes were provided for detailed analysis.

### KanbanBoard

KanbanBoard is an ASP.NET Core web API backed by SQLite (via Entity Framework Core) that manages kanban-style project boards with projects, epics, work items, comments, and epic documents. It supports optional OpenID Connect authentication with cookie sessions, CSRF protection, and personal access tokens (PAT) for API-key-style authentication. A companion MCP (Model Context Protocol) tool class exposes the entire API surface to LLM agents via typed server tools.

