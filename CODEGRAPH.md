# KanbanBoard

> **Confidence**: Medium
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-26 16:04 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

KanbanBoard is a full-stack project management system built on ASP.NET Core, organized as a multi-project .NET solution. The core project provides a REST API for managing kanban board entities including projects, epics, work items, comments, and user accounts, backed by MariaDB (with a SQLite-based legacy migration path). It includes OIDC authentication, CSRF protection, personal access tokens (PAT) for programmatic API access, and a real-time board change notification system. The solution is composed of several cooperating projects: KanbanBoard.Api serves as the HTTP entry point exposing REST endpoints; KanbanBoard.Shared provides common models, interfaces, or data transfer objects shared across the solution; KanbanBoard.DataMigrator is a standalone utility for applying database schema migrations (likely Entity Framework-based); KanbanBoard.Mcp integrates the Model Context Protocol (MCP), exposing kanban operations as LLM-callable tools so AI assistants can interact with board data programmatically; and KanbanBoard.Tests contains the unit and/or integration test suite. The repository has no identified cross-repository dependencies, functioning as a self-contained system. Nothing external is known to depend on it.

## Projects

### KanbanBoard.Api

KanbanBoard.Api is the web API layer of a Kanban board application. It exposes HTTP endpoints for managing Kanban board data and serves as the entry point for client requests, coordinating between the presentation layer and underlying business logic or data access components within the repository.

### KanbanBoard.Mcp

KanbanBoard.Mcp appears to be a Model Context Protocol (MCP) integration project within the KanbanBoard repository. Based on the project name, it likely exposes KanbanBoard functionality as MCP tools or resources, enabling AI assistants to interact with kanban board data. The graph contains no node definitions, suggesting the project may be minimal, empty, or its nodes were not captured in the analysis.

### KanbanBoard.Shared

KanbanBoard.Shared appears to be a shared library project within a Kanban board application repository, intended to contain common models, interfaces, or utilities shared across other projects (such as client and server). Based on the graph having no listed nodes, it likely serves as a foundational layer providing shared data transfer objects, domain models, or contracts used by other projects in the solution.

### KanbanBoard.DataMigrator

KanbanBoard.DataMigrator is a utility project responsible for applying database schema migrations to the KanbanBoard application's data store. It likely serves as a standalone executable or tool that runs Entity Framework or similar migration commands to keep the database schema in sync with the application's data models.

### KanbanBoard.Tests

KanbanBoard.Tests is the test project for the KanbanBoard repository, responsible for validating the correctness of the Kanban board application's logic and components. It contains unit and/or integration tests that exercise the core functionality of the system. The project serves as a quality assurance layer within the repository.

### KanbanBoard

KanbanBoard is a full-stack project management API and tooling suite built on ASP.NET Core, backed by MariaDB (with a SQLite-based legacy migration path). It provides REST endpoints for managing projects, epics, work items, comments, and user accounts, with optional OIDC authentication, CSRF protection, personal access tokens (PAT) for API access, a real-time board change notification system, and an MCP (Model Context Protocol) server layer that exposes kanban operations as LLM-callable tools.

